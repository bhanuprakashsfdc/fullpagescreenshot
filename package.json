{
  "name": "fullpage-screenshot",
  "version": "1.0.0",
  "description": "Capture full-page screenshots with annotations — no login, no signup, fully offline.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/watch.js",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
